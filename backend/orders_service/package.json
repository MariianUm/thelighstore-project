{
  "name": "orders-service",
  "version": "1.0.0",
  "scripts": {
    "start": "flask run --port=5001",
    "dev": "cross-env FLASK_APP=app.py FLASK_ENV=development flask run --host=0.0.0.0 --port=5001",
    "init-db": "flask db upgrade"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}

