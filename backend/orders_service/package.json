{
  "name": "orders-service",
  "version": "1.0.0",
  "scripts": {
    "start": "flask run --port=5001",
    "dev": "FLASK_APP=app:app FLASK_DEBUG=1 flask run --host=0.0.0.0 --port=5001",
    "init-db": "flask db upgrade"
  },
  "dependencies": {
    "flask": "^0.0.0",
    "flask-sqlalchemy": "^0.0.0",
    "flask-migrate": "^0.0.0",
    "psycopg2-binary": "^0.0.0",
    "requests": "^0.0.0"
  }
}
